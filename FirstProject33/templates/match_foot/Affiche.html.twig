{% extends 'back/base.html.twig' %}
{% block body%}

    <h1> liste de Match </h1>

    <td>
        <a class="btn btn-success" href=" {{ path('ListQB') }}"> Trie Par NbrSpectateur </a>
    </td>

    <table border="1" >
        <tr>
            <th>ref_match</th>
            <th>date_match</th>
            <th>nom_stade</th>
            <th>nbr_spectateur</th>


            <td> Delete</td>
            <td> Update</td>
        </tr>
        <tr {% for m in match_foot %}>
            <td>{{ m.getRefMatch() }}</td>
            <td>{{ m.getDateMatch() }}</td>
            <td>{{ m.getNomStade()}}</td>
            <td>{{ m.getNbrSpectateur()}}</td>
            <td> <a href="{{ path('dm',{ref_match:m.getRefMatch() }) }}">Delete</a></td>
            <td> <a href="{{ path('update1',{'ref_match':m.getRefMatch()}) }}">update</a></td>

        </tr {% endfor %}>
    </table>

{% endblock %}